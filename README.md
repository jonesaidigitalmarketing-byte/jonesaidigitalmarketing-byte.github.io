# Jones AI website preview

Temporary preview of the Jones AI Business Automations & Solutions website. Every page carries `noindex, nofollow`. The finished site lives on Wix.

Photos: drop them into the `images/` folder using the file names listed in `images/README.md`.
